apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: edvin-

resources:
  - ../../../../bases/grav

patches:
  - path: vault-patch.yaml
  - target:
      kind: Ingress
      name: grav-ingress
    patch: |-
      - op: replace
        path: /spec/rules/0/host
        value: edvin.test.ca
      - op: replace
        path: /spec/tls/0/hosts/0
        value: edvin.test.ca
  - target:
      kind: RandomSecret
    patch: |-
      - op: replace
        path: /spec/path
        value: kv/data/edvin-grav/default/grav
