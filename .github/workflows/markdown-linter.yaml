name: Markdown lint

on:
  workflow_call:
  pull_request:
    paths:
      - 'wiki/**'
    types:
      - opened
      - synchronize

jobs:
  markdown-repo-standards:
    uses:
      clubcedille/cedille-workflows/.github/workflows/markdown-lint.yml@master
