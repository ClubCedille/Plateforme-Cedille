apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: {{user}}-
namespace: {{user}}-sandbox

resources:
- ../../bases/sandbox

patches:
  - patch: |-
      - op: replace
        path: /subjects/0/name
        value: {{email}}
    target:
      kind: RoleBinding