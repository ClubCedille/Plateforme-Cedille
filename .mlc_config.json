{
    "aliveStatusCodes": [999,200,403, 429, 0],
    "ignorePatterns": [
        {
            "pattern": "^http://localhost"
        },
        {
            "pattern": "^https://localhost"
        }
    ]
}
